export default class User{
 
    constructor(id,name,email, access_token) {
        this.id = id;
        this.name = name
        this.email = email;
        this.access_token = access_token;

      }

}